[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['cd civic-backend && npm install --production=false']

[phases.build]
cmds = ['echo "No build step needed for backend"']

[start]
cmd = 'cd civic-backend && node index.js'
